#!/bin/bash
set -euo pipefail

ROOT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)

if [[ "${TEXTD_SKIP_BUILD:-0}" != "1" ]]; then
  "$ROOT_DIR/bin/build"
fi

java -cp "$ROOT_DIR/build/main-classes" \
  io.harbor.textd.cli.HotReloadIntegrationRunner \
  "$ROOT_DIR/build/plugins/customer-default.jar" \
  "${1:-12}"
